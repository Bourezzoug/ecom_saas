# Root compose settings. Copy to .env (git-ignored).

# Enable the NVIDIA GPU override for Ollama. Separator is ";" on Windows, ":" on macOS/Linux.
# COMPOSE_PATH_SEPARATOR=;
# COMPOSE_FILE=docker-compose.yml;docker-compose.gpu.yml

APP_PORT=8000
VITE_PORT=5173
WP_PORT=8081
OLLAMA_PORT=11434
MAILPIT_UI_PORT=8025
FORWARD_DB_PORT=54320
FORWARD_REDIS_PORT=63790
REVERB_HOST_PORT=8090
